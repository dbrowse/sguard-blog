

<table class="table table-hover col-lg-6">

         <thead>
            <tr>
              <th scope="col">Topic</th>
              <th scope="col">Time</th>
            </tr>
         </thead>
      <% @posts.each do |post, num| %>
         <tbody>
           <tr>


             <td scope="row"><%= link_to post.title, post %></td>
             <td scope="row"><%= post.created_at.strftime("%B, %d, %Y") %></td>
            </tr>
          </tbody>
    <% end %>
  </table>
